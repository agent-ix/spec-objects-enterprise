{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agent-ix.org/agent-ix/spec-objects-enterprise/0.2.0/IdentityField.json",
  "type": "object",
  "properties": {
    "identity": {
      "type": "boolean",
      "const": true
    }
  },
  "required": [
    "identity"
  ],
  "description": "Open marker: a `FieldDecl` flagged as identity. Deliberately open so it\nmatches any identity-flagged field."
}
